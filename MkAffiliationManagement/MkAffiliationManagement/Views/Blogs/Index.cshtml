@using Microsoft.AspNetCore.Identity
@using MkAffiliationManagement.Areas.Identity.Data
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@model IEnumerable<MkAffiliationManagement.Models.Blog>

@{

    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row py-5">
    <div class="col-lg-9 mx-auto text-white text-center">
        <h1 class="display-4">Blog</h1>


    </div>
</div>
@if (SignInManager.IsSignedIn(User) && User.IsInRole(ApplicationRoles.Admin))
{


    <div class="row py-5">
        <div class="col-lg-9 mx-auto text-white text-center">
            <a class="col btn btn-dark-moon btn-rounded text-center" style="display: flex; flex-direction: column; justify-content: center; text-align: center; height:100px;
" asp-action="Create">Create New</a>


        </div>
    </div>
}
@*<table class="table" style="background:white; border:solid black 2px;">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Body)
                </th>
                <th>

                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Date)
                </th>
                @if (SignInManager.IsSignedIn(User) && User.IsInRole(ApplicationRoles.Admin))
                {
                    <th></th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.ID">
                            @Html.DisplayFor(modelItem => item.Title)
                        </a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Body)
                    </td>
                    <td>
                    </td>
                    <td>
                        <b>
                            @Html.DisplayFor(modelItem => item.Date)
                        </b>
                    </td>
                    @if (SignInManager.IsSignedIn(User) && User.IsInRole(ApplicationRoles.Admin))
                    {
                        <td>
                            <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.ID">Edit</a>
                            <a class="btn btn-info" asp-action="Details" asp-route-id="@item.ID">Details</a>
                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>*@

<div class="container" style=" justify-content:center; display:flex; flex-wrap: wrap; gap:10px;">
    @foreach (var item in Model)
    {

        <div class="card" style="width: 20rem;">
            <div class="card-body">
                <h5 class="card-title">
                    <b>
                        @Html.DisplayFor(modelItem => item.Title)

                    </b>
                   
                </h5>
                <h6 class="card-subtitle mb-2 text-muted">@Html.DisplayFor(modelItem => item.Date)</h6>
                <p class="card-text" style="        
                white-space: nowrap;
                overflow: hidden;
                text-overflow: ellipsis;
                max-width:inherit; ">@Html.DisplayFor(modelItem => item.Body)</p>
                <a  asp-action="Details" asp-route-id="@item.ID" class="card-link btn btn-primary float-left stretched-link">Read More</a>
                <a class="btn btn-secondary float-right" href="">Share</a>
            </div>
        </div>


    }
</div>
